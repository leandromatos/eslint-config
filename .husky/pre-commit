# run lint-staged to check for linting errors
pnpm exec lint-staged --verbose

# verify the lockfile is in sync with package.json
pnpm install --frozen-lockfile

# run the test suite before allowing the commit
pnpm run test
