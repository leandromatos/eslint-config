{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Basic Options
    "types": ["node"],

    // Emit Options
    "rootDir": "./src",
    "sourceMap": false
  },
  "exclude": ["**/*.spec.ts", "dist", "eslint.config.mts", "fixtures", "node_modules", "src/**/__tests__"]
}
